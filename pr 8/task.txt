Практическая №8

Продвинутые методы работы с DOM в JavaScript.

Задание

Написать скрипт согласно заданию. Разрешаетcя использовать только идентификатор let. jQuery использовать нельзя. Для заданных преобразований не использовать псевдоэлементы и псевдоклассы.

Запрещается использовать простой поиск элементов (getElementById(), querySelector() и т.д), но возможно применение события при:

document.getElementById("test").addEventListener("click", function( event ) {

event.target.innerHTML = "click count: " + event.detail;

и в других подобных случаях, когда это вытекает из логики решения задачи.

Нельзя вешать прямые обработчики на элементы (onfocus(), onmouseover() и т.д.).

Следует использовать обработчики событий типа: addEventListener() stopPropagation() и т.д.

[Данные методы используются для расширяемых проектов, и позволяют создавать скрипты, менее зависимые от кода html]

В отчёте предоставить код js.

15. Напишите скрипт для страницы из 3й практической работы, который при наведении на любые наушники курсора начинает проигрывать музыку, пока курсор не покинет область.